name: Reusable

on:
  workflow_call:
    inputs:
      some-input:
        description: "Some env var"
        required: true
        type: string

env:
  SOME_ENV_VAR: ${{ inputs.SOME_ENV_VAR }}
  OTHER_VAR: ${{ github.event.before }}

jobs:
  something-that-fails:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.some-input }}
      - run: echo "$OTHER_VAR"
