name: Pushing to main

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  something:
    runs-on: ubuntu-latest
    steps:
      - run: exit 1
      - name: check
        if: github.ref == 'main'
        run: echo 'is main'
        