name: Pushing

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      SOME_ENV_VAR:
        description: "A description of the input"
        required: true
        default: "this"

jobs:
  something:
    runs-on: ubuntu-latest
    steps:
      - name: Do something
        if: ${{ github.ref_name == 'main' }}
        run: echo "yesss"
