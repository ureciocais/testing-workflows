name: Pushing

on:
  - pull_request
  # - workflow_dispatch

jobs:
  something-that-fails:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/install
        with:
          should-run: ${{ contains(github.event.pull_request.labels.*.name, 'hellooooooo') }}
