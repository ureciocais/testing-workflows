name: Pushing

on:
  push

jobs:
  something:
    runs-on: ubuntu-latest
    steps:
      
      - name: Check out code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Paco
        timeout-minutes: 1
        run: |
          sleep 120

      - name: failure
        if: failure()
        run: echo "FAILLLLL"