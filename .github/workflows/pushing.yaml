name: Pushing

on: push

jobs:
  something-that-fails:
    runs-on: ubuntu-latest
    steps:
      - id: check-me
        run: |
          echo "one=no" >> $GITHUB_OUTPUT
          echo "two=no" >> $GITHUB_OUTPUT
      - name: cosa
        if: ${{ steps.check-me.outputs.one == 'yes' || steps.check-me.outputs.two == 'no' }}
        run: |
          echo "oyemeee"
