name: Pushing

on:
  push:
  workflow_dispatch:
    inputs:
      some-input:
        description: "A description of the input"
        required: true
        default: "this"

jobs:
  something:
    uses: cais-group/fe-pipelines/.github/workflows/reusable.yaml@main
