name: Pushing

on:
  push

jobs:
  something:
    runs-on: ubuntu-latest
    steps:
      
      - name: Check out code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Debugging with ssh
        uses: lhotari/action-upterm@v1

