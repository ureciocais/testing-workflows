name: Pushing

on: push

jobs:
  something-that-fails:
    runs-on: ubuntu-latest
    if: ${{ vars.RUN_IT == 'true' }}
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/actions/install
