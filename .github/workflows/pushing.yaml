name: Pushing

on:
  push:
  workflow_dispatch:
    inputs:
      some-input:
        description: "A description of the input"
        required: true
        default: "A default value"

jobs:
  something-that-fails:
    runs-on: ubuntu-latest
    steps:
      # - id: check-me
      #   run: |
      #     echo "varr='this'" >> $GITHUB_OUTPUT
      # - run: echo ${{ steps.check-me.outputs.varr }}
      - name: "should echo?"
        if: ${{ inputs.some-input != 'this' }}
        run: echo 'yesss?'
